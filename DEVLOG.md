# medi365 개발 기록

> 2026-09-06 작업 기록. 무엇을 만들었고, 왜 그렇게 정했고, 무엇이 남았는지.
> 기술 참고 문서는 [README.md](README.md) 를 본다.

- **배포**: https://medi365.vercel.app
- **저장소**: https://github.com/lee-kwang-jae/medi365
- **스택**: React 18 + Vite + Tailwind CSS 3 / Kakao Maps SDK / 응급의료포털(E-Gen) 공공데이터
- **커밋**: 29개

---

## 1. 무엇을 만들었나

지금 이 시간 **문 연 약국·병의원을 반경 3km 안에서** 찾아 거리순으로 보여주는 웹앱.

### 화면

| | 내용 |
| --- | --- |
| 탭 | `💊 약국` · `🏥 병의원` · `🧒 소아` |
| 검색 | 지역명 입력 + 현재 위치. **구글 지도처럼 지도 위에 떠 있음** |
| 지도 | 카카오맵. 검색 위치 + 반경 원 + 결과 마커 |
| 목록 | 상호명·종별·영업상태·주소·영업시간·전화·거리 |
| 상세 | 카드/마커 클릭 시 **그 장소만 담은 전용 지도**가 있는 시트 |
| 외부 연동 | 카카오맵으로 보기 · 길찾기 · 전화 |

### 판정 로직

1. **좌표 확정** — 검색어는 `Geocoder.addressSearch` → 실패 시 `Places.keywordSearch`.
   최초 진입은 Geolocation, 권한 거부 시 성남시청.
2. **후보 수집** — 정적 데이터셋(1순위) 또는 E-Gen API(폴백).
3. **시각 필터** — 응답의 `dutyTime1s~8c`(요일별 시작/종료)와 현재 `HHMM` 비교.
   종료 < 시작이면 심야영업(익일)으로 보고, 전날 심야영업이 새벽까지 이어지는 경우도 포함.
4. **반경 필터·정렬** — Haversine 으로 거리를 구해 3km 이내만 남기고 가까운 순.

---

## 2. 왜 그렇게 정했나

### 반경이 행정구역 경계를 넘는 문제

E-Gen 은 시/도(`Q0`) + 시군구(`Q1`) 단위로만 조회된다. 검색 지점의 시군구만 부르면
경계 근처에서 가까운 시설이 통째로 사라진다. **중심 + 8방위 반경 지점**의 행정구역을
모두 구해 병렬 조회하도록 했다. (정적 데이터셋으로 넘어가면 이 문제 자체가 없어진다)

### `QT31~QT48` 은 요청 파라미터가 아니다

요청의 `QT` 는 **요일까지만** 좁힌다. 시:분 단위 영업 여부는 응답 필드
`dutyTime{n}s` / `dutyTime{n}c` 로 클라이언트가 판정해야 한다.

### 소아 탭 — 클라이언트 필터가 불가능하다

병의원 목록 **응답에 진료과목 필드가 없다.** 실제 응답을 덤프해 확인했다.

```
dutyAddr, dutyDiv, dutyDivNam, dutyEmcls, dutyEmclsName, dutyEryn,
dutyName, dutyTel1, dutyTime1s~7c, hpid, postCdn1/2, wgs84Lat/Lon
```

그래서 요청 파라미터 `QD` 로만 좁힐 수 있다. `D002` = 소아청소년과 는 **역추적으로 확정**했다.
코드마스터 API 는 활용신청이 안 돼 403 이라, 이름에 `소아청소년과` 가 든 강남구 기관 5곳이
어느 코드에 속하는지 확인했다.

| 코드 | 포함 | 강남구 total |
| --- | --- | --- |
| **D002** | **5/5** | 232 |
| D001 | 4/5 | 500 |
| D005 | 2/5 | 1161 |

분당구 148 / 해운대구 118 로 규모도 타당하고, 이름 기준 소아계열 비율이 18~22% 로 재현된다.

### `365` 이름 규칙

`365` 계열 의원은 소아진료도 한다는 확인에 따라 `QN=365` 를 합집합으로 넣되,
소아진료와 무관한 곳을 걸렀다.

- 종별(`dutyDivNam`)이 치과·한의원·한방이면 제외 — `강남365치과의원`, `바로365한의원`, `365매일한방병원`
- 이름에서 **`365` 뒤쪽**에 영문자·`엠씨`·특수 진료과가 오면 제외 — `365MC의원`,
  `365엠씨의원`, `365판교튼튼재활의학과의원`, `모두탑365정형외과의원`
- `가정의학과`·`내과` 는 일반 진료라 제외하지 않음

이 규칙은 `QN=365` 갈래에만 적용한다. `D002` 로 잡힌 곳은 기관이 소아청소년과를 직접
등록한 곳이라 이름과 무관하게 유지한다.

### 명절 비상진료

설·추석에는 요일 시간표가 무의미해진다.
`HolidyEmgncClnicInsttInfoInqireService/getHolidyClnicPosblEgytInfoInqire` 를 함께 조회한다.

이 API 응답에는 **좌표가 없어서** 명절에는 목록 API 를 **요일 필터 없이** 불러 전체 좌표를
확보한 뒤 `hpid` 로 조인한다. 요일 필터를 두면 "평소 그 요일엔 안 열지만 명절엔 여는 곳"이
서버 단계에서 사라져 조인 대상 자체가 없어진다.

개발계정 트래픽이 1,000회/일로 빠듯해 `HOLIDAY_SEASONS` 에 등록된 연휴 당일에만 호출한다.

### 마커 기하

| 상태 | 형태 | 크기 | 앵커 |
| --- | --- | --- | --- |
| 미선택 | 원형 | 28 × 28 | 원의 중심이 좌표 |
| 선택 | 물방울형 | 56 × 67.59 | 아래 꼭짓점이 좌표 |

정사각형에 `border-radius: 50% 50% 50% 0` + `rotate(-45deg)` → 너비 S, 높이 1.207S.
`CustomOverlay` 는 생성 후 앵커를 바꿀 수 없어서(`setYAnchor` 가 없다), 오버레이 콘텐츠를
**0×0 박스로 좌표에 고정**하고 그 안의 마커를 `transform` 으로 옮긴다. 앵커 변화가 곧
`transform` 변화가 되어 모양·크기가 함께 애니메이션된다.

---

## 3. 겪은 문제와 해결

### 지도 축척이 최대로 축소되던 문제

반경 3km 결과인데 한반도 전체가 보였다. 계측 결과:

```
boxBefore={w:0,h:0}  levelBefore=6 → levelAfterSync=14
bounds 자체는 정상 (37.360~37.487, 127.099~127.211)
```

컨테이너가 0×0 일 때 `setBounds` 를 부르면 카카오 지도가 최대 축소로 계산한다.
**중간에 한 번 틀렸다** — `requestAnimationFrame` 으로 미뤘는데, rAF 는 숨겨진 탭에서
실행되지 않아 bounds 가 영영 적용되지 않는 더 나쁜 코드였다.
최종 해법은 bounds 를 보류했다가 `ResizeObserver` 로 크기가 생기는 순간 적용하는 것.

### 데스크톱 레이아웃 매직 넘버

`calc(100vh - 108px)` 이 실제 헤더 높이(109px)와 어긋나 13px 넘쳤다.
flex 로 남은 공간을 채우게 바꿔 숫자 의존을 없앴다. 이 과정에서
`[@supports(height:1dvh)]:h-[52dvh]` 가 폭 제한 없이 생성돼 **모든 화면에서 `lg:h-full` 을
덮고 있던** 버그도 드러나 `max-lg:` 로 한정했다.

### 환경변수가 빈 값으로 저장되던 문제

Vercel 대시보드에서 만든 변수 두 개가 값이 비어 있었다. 런타임 진단:

```
{"matchedNames":["EGEN_SERVICE_KEY","VITE_KAKAO_JS_KEY"],
 "lengths":{"EGEN_SERVICE_KEY":0,"VITE_KAKAO_JS_KEY":0}}
```

원인은 Vercel 이 알려준 규칙이었다 — `VITE_` 접두사 변수는 어차피 브라우저에 노출되므로
**Sensitive(secret) 저장을 거부**한다. `--no-sensitive --visibility config` 로 재등록해 해결.

### 인증키를 서버로 이관

`VITE_EGEN_SERVICE_KEY` 는 빌드 시 클라이언트 번들에 그대로 박힌다.
`VITE_` 접두사를 떼어 `EGEN_SERVICE_KEY` 로 바꾸고, 개발은 Vite dev 프록시가,
배포는 `api/egen.js` 가 요청에 키를 주입하도록 했다. 빌드 산출물을 grep 해 키가 없음을 확인했다.

---

## 4. 가장 큰 문제 — 상류(data.go.kr) 불안정

하루 종일 502 / 504 / 자체 오류코드 `05`(SERVICETIMEOUT) 가 간헐적으로 발생했다.
지역명이나 파라미터 문제가 아니다.

**시군구 표기는 문제가 없다.** E-Gen 이 자기 주소 데이터에서 쓰는 표기 213개를 수집해
대조했고 우리가 만드는 표기와 일치한다 (`성남시분당구`, `전주시완산구`, `창원시마산회원구`,
인천 신설 `영종구`·`검단구`). `강원특별자치도 춘천시` → 373건, `경기도 하남시` → 병의원
463건 정상.

### 대응 1 — 재시도 (전국 21개 시군구 실측)

```
단일 시도 성공    4/21
재시도 적용 성공 16/21     ← 4배
```

5xx·네트워크 오류·코드 01/04/05 에만 최대 2회(400ms, 1200ms). 인증 오류나 잘못된
파라미터는 재시도하지 않는다. 요청당 10초 타임아웃.

### 대응 2 — 부분 결과 + 재시도 버튼

일부 시군구만 실패하면 받아온 만큼 보여주고 `⚠ 일부 지역(n/m)` 을 알린다.
전부 실패하면 `↻ 다시 시도` 버튼.

### 대응 3 — 성공 응답만 캐시 (5분)

기존 코드는 오류에도 캐시 헤더를 붙이고 있었다. 그대로 시간만 늘렸다면 **장애가 5분간
고정**됐을 것이다. 응급의료포털은 오류를 `HTTP 200 + 오류 XML` 로 내려주기도 해서
상태 코드만으로는 판단할 수 없다.

```
오류 응답  → cache-control: no-store,  x-vercel-cache: MISS  (3회 연속 확인)
성공 응답  → s-maxage=300,             x-vercel-cache: HIT   (3회 연속 확인)
```

### 대응 4 — 정적 데이터셋 (근본 해결)

17:13 경 `apis.data.go.kr` **게이트웨이가 통째로 다운**됐다. 포털 웹사이트는 정상인데
API 만 TCP 연결조차 안 됐고, 로컬과 Vercel 두 네트워크에서 동일했다. 재시도로도 못 막는다.

이 데이터는 하루에 한 번 바뀔까 말까 한다. 검색할 때마다 물어볼 이유가 없다.

```
기존:  사용자 검색 → E-Gen 호출         → 상류 죽으면 실패
변경:  하루 1회 미리 받아 정적 파일로   → 검색은 우리 파일만 읽음 → 오류 0
```

**행정구역 대신 좌표 격자(0.1도 ≈ 10km)로 나눈다.** 지역명 대조가 불필요해지고,
반경 3km 면 격자 1~4개만 읽으면 된다. 카카오 8방위 탐색도 사라진다.

소아 탭은 진료과목이 서버 필터라 정적 파일만으로 못 거른다. 수집기가 `D002` 등록 기관의
`hpid` 목록을 따로 받아두고 앱이 그걸로 거른다.

수집기는 **원자적**이다. 처음엔 한 시도가 실패하면 약국은 새 데이터로 덮이고 병의원은
실패해 반쪽 상태가 될 수 있었다. 지금은 전부 모은 뒤 한 번에 기록한다.
**하루 거르는 편이 데이터 훼손보다 낫다.**

---

## 5. 현재 상태

| 항목 | 상태 |
| --- | --- |
| 앱 기능 (지도·검색·목록·상세·마커) | ✅ 완료 |
| 재시도 / 부분 결과 / 캐시 정책 | ✅ 완료 |
| 정적 데이터셋 읽기 구조 | ✅ 완료 (데이터 없으면 API 폴백) |
| 수집기 `scripts/fetch-data.mjs` | ✅ 완료 |
| GitHub Actions 일 1회 갱신 | ✅ 완료 |
| `EGEN_SERVICE_KEY` 시크릿 | ✅ 등록 |
| Vercel Git 자동 배포 | ✅ 확인 (푸시 후 5~7초) |
| **전국 데이터 수집 실행** | ⛔ **상류 다운으로 대기 중** |

데이터가 채워지기 전까지는 실시간 API 로 폴백하므로, 상류가 죽어 있는 동안은 검색이 안 된다.

### 남은 일

1. 상류 복구 시 `node scripts/fetch-data.mjs` 실행 → 데이터 커밋 (또는 내일 03:00 자동)
2. 데이터 반영 후 실제 용량·응답속도 확인
3. (선택) `달빛어린이병원 및 소아전문센터` API 연동 — 야간·휴일 소아 지정기관이라
   지금의 `365` 이름 규칙보다 정확하다

---

## 6. 운영

### 로컬 실행

```bash
npm install
cp .env.example .env   # 키 입력 후
npm run dev
```

| 변수 | 노출 | 발급처 |
| --- | --- | --- |
| `VITE_KAKAO_JS_KEY` | 브라우저 | 카카오 개발자센터 → JavaScript 키 |
| `EGEN_SERVICE_KEY` | **서버만** | 공공데이터포털 → 일반 인증키(Decoding) |

카카오 개발자센터 → 플랫폼 → Web 사이트 도메인에 `http://localhost:5173` 과
`https://medi365.vercel.app` 등록 필요.

### 배포

`main` 에 push → Vercel 자동 배포 (5~7초). 수동 배포는 불필요.

### 데이터 갱신

매일 03:00 KST GitHub Actions 가 수집 → 변경된 격자만 커밋 → push → 자동 배포.
수동 실행은 Actions 탭 → `응급의료포털 데이터 갱신` → Run workflow.

### 조정 지점

| 값 | 위치 |
| --- | --- |
| 검색 반경 | `SEARCH_RADIUS_KM` in `src/lib/constants.js` (현재 3km) |
| 지도 초기 축척 기준 | `FIT_NEAREST_COUNT` in `src/components/KakaoMap.jsx` (현재 5곳) |
| 마커 크기 | `--d` in `src/index.css` (현재 28px) |
| 공휴일·명절 | `HOLIDAYS` / `HOLIDAY_SEASONS` in `src/lib/constants.js` — **매년 갱신 필요** |
| 소아 필터 규칙 | `TABS` 의 `pediatric` 항목 in `src/lib/constants.js` |

---

## 7. 알려진 한계

- **영업시간은 기관이 등록한 정보**라 실제와 다를 수 있다. 방문 전 전화 확인 안내를 하단에 두었다.
- **`병의원` 탭은 종별로 자르지 않는다.** 상급 병원을 숨기면 야간·휴일에 실제로 갈 수 있는
  곳이 사라진다. 대신 카드에 종별 배지를 단다.
- **소아 탭에 `D002` 로 잡힌 산부인과·한방병원 등이 보일 수 있다.** 기관이 소아청소년과를
  직접 등록한 곳이라 E-Gen 기준으로는 소아진료를 한다.
- **`365` 규칙은 이름 기반 휴리스틱**이라 소아진료를 안 하는 곳이 들어올 수 있다.
  `판교이엠365의원` 처럼 `365` **앞**에 영문 음차가 오는 경우는 규칙이 뒤쪽만 보므로 통과한다.
- **공휴일 목록이 하드코딩**되어 있다. 정확도가 중요하면 공공데이터포털
  「특일 정보(getRestDeInfo)」 로 대체할 것. 목록에 없는 날은 평일 시간표로 동작한다.
- **이모지 마커는 시스템 폰트를 따른다.** OS 마다 모양이 조금씩 다르다.
