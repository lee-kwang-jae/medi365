name = "medi365-egen"
main = "egen-proxy.js"
compatibility_date = "2025-01-01"

[vars]
# 이 프록시를 호출할 수 있는 오리진 (쉼표 구분)
ALLOWED_ORIGINS = "https://lee-kwang-jae.github.io,http://localhost:5173"

# EGEN_SERVICE_KEY 는 vars 가 아니라 secret 으로 등록한다:
#   npx wrangler secret put EGEN_SERVICE_KEY
