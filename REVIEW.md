# 검토 요청

이 저장소는 **지금 이 시간 문 연 약국·병의원을 반경 3km 안에서 찾아주는 웹앱**입니다.
앱 자체는 동작하지만, **데이터 제공처가 불안정해 검색이 자주 실패**합니다.
그 진단과 대응이 타당한지 봐주시면 감사하겠습니다.

- 배포: https://medi365.vercel.app
- 전체 개발 기록: [DEVLOG.md](DEVLOG.md)
- 기술 문서: [README.md](README.md)

---

## 1. 증상

응급의료포털(E-Gen, `apis.data.go.kr`) 호출이 간헐적으로 실패합니다.

```
HTTP 504  <errMsg>SERVICETIMEOUT_ERROR</errMsg>  <returnReasonCode>05</returnReasonCode>
HTTP 502
HTTP 500  code=01 (APPLICATION_ERROR)
연결 자체 실패 (UND_ERR_CONNECT_TIMEOUT)
```

2026-09-06 하루 동안 계속 반복됐고, 17:13경에는 **게이트웨이가 통째로 다운**됐습니다.

| 확인 항목 | 결과 |
| --- | --- |
| `www.data.go.kr` (포털) | HTTP 200, 180ms — 정상 |
| `apis.data.go.kr` (API) | TCP 연결 타임아웃 |
| 로컬 PC | 실패 |
| Vercel (다른 국가/IP) | 동일하게 실패 |

두 개의 독립된 네트워크에서 동일해 **IP 차단이 아니라 상대편 장애**로 판단했습니다.

## 2. 배제한 원인

- **지역명 표기 오류가 아님** — E-Gen 이 자기 주소 데이터에서 쓰는 시군구 표기 213개를
  수집해 대조했고, 우리가 `coord2RegionCode` 결과에서 만드는 표기와 일치합니다
  (`성남시분당구`, `전주시완산구`, `창원시마산회원구`, 인천 신설 `영종구`·`검단구`).
  `강원특별자치도 춘천시` → 373건, `경기도 하남시` → 병의원 463건 정상 조회됩니다.
- **응답 크기 문제가 아님** — `numOfRows` 를 100~1000 으로 바꿔가며 8회씩 측정했으나
  일관된 상관관계가 없었습니다 (1000행 6/8, 300행 2/8 — 노이즈가 지배적).
- **트래픽 초과가 아님** — 한도 초과면 코드 `22` 가 오는데 관측된 건 `05`(연결실패)입니다.

## 3. 적용한 대응

| 단계 | 내용 | 효과 |
| --- | --- | --- |
| 재시도 | 5xx·네트워크·코드 01/04/05 에만 최대 2회(400/1200ms) | 전국 21개 시군구 실측 **4/21 → 16/21** |
| 부분 결과 | 일부 지역 실패 시 받아온 만큼 표시 + 경고 | 조용한 0건 방지 |
| 캐시 | **성공 응답만** `s-maxage=300` / 오류는 `no-store` | 장애 고정 방지 |
| 정적 데이터셋 | 하루 1회 미리 받아 좌표 격자 파일로 배포 | 상류 의존 제거 |

### 캐시에서 조심한 부분

기존 코드는 오류 응답에도 캐시 헤더를 붙이고 있었습니다. 이 API 는 오류를
`HTTP 200 + 오류 XML` 로 내려주기도 해서 상태 코드만으로 판단하면 CDN 이 오류를
정상 응답으로 저장합니다. 그대로 캐시 시간을 늘렸다면 장애가 5분간 고정됐을 것입니다.

## 4. 봐주셨으면 하는 것

1. **상류 장애 진단이 맞는지** — `05 SERVICETIMEOUT` 을 "게이트웨이가 원본 서버
   (국립중앙의료원)로부터 제때 응답을 못 받은 것"으로 읽었습니다. 다른 해석이 가능한지.
2. **정적 데이터셋 전환이 과한 대응인지** — 데이터가 하루 단위로 바뀌므로 실시간 호출을
   없애는 게 맞다고 판단했습니다. 놓친 부작용이 있는지.
3. **좌표 격자 분할(0.1도)이 적절한지** — 행정구역 이름 대조를 피하려는 선택입니다.
   격자 크기나 경계 처리에 문제가 없는지. → `src/lib/dataset.js`, `scripts/fetch-data.mjs`
4. **미수집 지역 폴백 판정** — `index.json` 의 격자 목록에 필요한 격자가 하나라도 없으면
   실시간 API 로 폴백합니다. 이 판정에 구멍이 없는지. → `src/lib/dataset.js`
5. **영업 판정 로직** — 요일별 `dutyTime{n}s/c` 와 현재 시각 비교, 심야영업(익일) 처리,
   전날 심야영업 이월 처리. → `src/lib/time.js`

## 5. 재현 방법

```bash
npm install
cp .env.example .env    # 키 2개 입력 필요
npm run dev
```

키가 없으면 지도만 뜨고 검색이 실패합니다. 상류가 불안정할 때는 키가 있어도
간헐적으로 실패하는데, 그게 이 문서의 주제입니다.

> **`.env` 는 이 압축 파일에 포함되어 있지 않습니다.** 인증키가 들어 있어 제외했습니다.

## 6. 주요 파일

| 파일 | 역할 |
| --- | --- |
| `src/lib/egen.js` | E-Gen API 클라이언트 — 재시도·타임아웃·오류 판정 |
| `src/lib/dataset.js` | 정적 데이터셋 읽기 — 격자 계산·폴백 판정 |
| `src/lib/finder.js` | 데이터셋/API 선택 → 시각·반경 필터 → 정렬 |
| `src/lib/time.js` | 요일 코드·영업시간 판정 |
| `scripts/fetch-data.mjs` | 전국/부분 수집기 |
| `api/egen.js` | Vercel 프록시 — CORS 우회·키 주입·캐시 정책 |
| `.github/workflows/refresh-data.yml` | 일 1회 자동 갱신 |
